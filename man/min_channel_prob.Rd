% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/max_subtract_signature.R
\name{min_channel_prob}
\alias{min_channel_prob}
\title{Minimum per-channel probability that a Poisson draw does not exceed the
observed count.}
\usage{
min_channel_prob(n_subtract, sig, spectrum)
}
\arguments{
\item{n_subtract}{Number of mutations drawn.}

\item{sig}{Signature profile (numeric vector summing to 1).}

\item{spectrum}{Observed counts per channel (numeric vector).}
}
\value{
Minimum per-channel probability (scalar between 0 and 1).
}
\description{
For each channel \eqn{j}, computes
\code{P(Poisson(n_subtract * sig_j) <= spectrum_j)} and returns the
minimum across channels. Used with a \enc{Šidák}{Sidak}-corrected
per-channel threshold so the criterion is approximately invariant to the
number of channels.
}
\keyword{internal}
